# This folder contains the plugin source code, but the build tooling in `@grafana/toolkit`
# can fail on Windows due to legacy `node-sass` dependencies.
#
# Recommended: generate a fresh plugin using Grafana Plugin Tools and then copy this folder's `src/` into it.
#
# See README.md.
